/** Минимальный отступ центра проёма от концов стены (мм). */
export const OPENING_EDGE_MARGIN_MM = 120;

/** Минимальный зазор между проёмами на одной стене вдоль оси (мм). */
export const OPENING_MIN_GAP_ALONG_MM = 40;

export const OPENING_MAX_HEIGHT_MM = 6000;
export const OPENING_MAX_BOTTOM_OFFSET_MM = 5000;

/** Для двери в MVP низ проёма на «полу» — допуск по числу с плавающей точкой. */
export const DOOR_BOTTOM_OFFSET_EPS_MM = 1;
