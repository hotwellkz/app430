# 🔐 Система управления аккаунтами WhatsApp

## 🎯 **Описание проблемы и решения**

### **❌ Проблема ДО исправления:**
- Сообщения приходили от неизвестных номеров
- Нет контроля над активным аккаунтом
- Смешивание данных от разных аккаунтов в базе
- Невозможность чистого переключения между аккаунтами
- Старые сессии "висели" в системе

### **✅ Решение ПОСЛЕ исправления:**
- Строгий контроль одного активного аккаунта
- Система полной очистки данных при смене аккаунта
- Интерфейс управления аккаунтом
- Фильтрация сообщений по принадлежности к аккаунту
- Автоматическая очистка при отключении

---

## 🏗️ **Архитектурные изменения**

### **1. Backend (whatsapp-server)**

#### **Новые переменные управления аккаунтом:**
```typescript
let currentAccountInfo: {
    phoneNumber?: string;
    name?: string;
    profilePicUrl?: string;
    isReady: boolean;
    connectedAt?: string;
} = { isReady: false };
```

#### **Функции очистки данных:**
- `clearAccountData()` - очистка кэша чатов, read-статусов, аватарок
- `clearAuthFiles()` - удаление .wwebjs_auth и .wwebjs_cache папок
- `updateAccountInfo()` - обновление информации о подключенном аккаунте

#### **Новые API endpoints:**
```
GET  /whatsapp/account       - информация о текущем аккаунте
POST /whatsapp/reset         - полный сброс с очисткой данных
POST /whatsapp/soft-logout   - выход с сохранением данных
GET  /whatsapp/chats-summary - статистика чатов и аккаунта
```

#### **Фильтрация сообщений по аккаунту:**
```typescript
// Входящие сообщения
if (currentAccountInfo.isReady && msg.to === currentAccountInfo.phoneNumber) {
    await processIncomingMessage(msg);
} else {
    console.log('⚠️ Message not for current account, ignoring');
}

// Исходящие сообщения  
if (currentAccountInfo.isReady && msg.from === currentAccountInfo.phoneNumber) {
    await processOutgoingMessage(msg);
} else {
    console.log('⚠️ Outgoing message not from current account, ignoring');
}
```

### **2. Frontend**

#### **Новый компонент AccountManager:**
- Отображение информации о текущем аккаунте
- Статистика чатов и сообщений
- Кнопки "Выйти" и "Полный сброс"
- Модальные окна подтверждения

#### **Интеграция в WhatsAppUI:**
- Выдвижная панель управления аккаунтом
- Индикатор статуса подключения
- Автоматическое обновление при смене аккаунта

#### **Новые типы данных:**
```typescript
interface WhatsAppAccountInfo {
    phoneNumber?: string;
    name?: string;
    profilePicUrl?: string;
    isReady: boolean;
    connectedAt?: string;
}
```

---

## 🔄 **Socket.IO События**

### **Новые серверные события:**
- `account-connected` - подключение нового аккаунта
- `account-disconnected` - отключение аккаунта
- `account-reset` - сброс аккаунта
- `account-logout` - выход из аккаунта
- `account-auth-failed` - ошибка аутентификации

### **Обработка на фронтенде:**
```typescript
socket.on('account-reset', (data) => {
    setChats({});
    setActiveChat(null);
    alert('Аккаунт WhatsApp был сброшен. Требуется повторная аутентификация.');
});
```

---

## 🛠️ **Функции очистки данных**

### **1. chatStorage.ts - clearAllChats():**
```typescript
export const clearAllChats = async (): Promise<void> => {
    // Очищаем кэш
    chatsCache = {};
    
    // Очищаем из Supabase
    const { error } = await supabase
        .from('whatsapp_chats')
        .delete()
        .neq('id', '0');
}
```

### **2. readStatusStorage.ts - clearAllReadStatuses():**
```typescript
export const clearAllReadStatuses = (): boolean => {
    // Удаляем файл статусов
    if (fs.existsSync(READ_STATUS_FILE)) {
        fs.unlinkSync(READ_STATUS_FILE);
    }
    return true;
}
```

### **3. Очистка файлов аутентификации:**
```typescript
const clearAuthFiles = async (): Promise<void> => {
    const authPath = path.resolve(__dirname, '../.wwebjs_auth');
    const cachePath = path.resolve(__dirname, '../.wwebjs_cache');
    
    await fs.rm(authPath, { recursive: true, force: true });
    await fs.rm(cachePath, { recursive: true, force: true });
}
```

---

## 🎮 **Интерфейс управления аккаунтом**

### **Кнопка "Управление аккаунтом" в заголовке:**
- Показывает/скрывает панель управления
- Индикатор статуса (Подключен/QR-код/Отключен)

### **Панель AccountManager содержит:**

#### **📊 Информация об аккаунте:**
- Аватарка пользователя
- Имя и номер телефона
- Время подключения
- Статус подключения

#### **📈 Статистика чатов:**
- Всего чатов
- Количество сообщений
- Непрочитанные чаты
- Записи статусов прочитанности

#### **🔧 Действия:**
- **"Выйти из аккаунта"** (желтая кнопка):
  - Выполняет `soft-logout`
  - Сохраняет все чаты и данные
  - Требует повторной аутентификации
  
- **"Полный сброс"** (красная кнопка):
  - Выполняет `reset`
  - Удаляет ВСЕ данные
  - Очищает кэш аутентификации
  - Требует сканирования нового QR-кода

---

## 🔄 **Сценарии использования**

### **Сценарий 1: Смена аккаунта с сохранением данных**
1. Пользователь нажимает "Выйти из аккаунта"
2. Подтверждает действие в модальном окне
3. Система выполняет `soft-logout`
4. Чаты сохраняются, но требуется новая аутентификация
5. Пользователь сканирует QR-код нового аккаунта
6. Новые сообщения фильтруются по новому аккаунту

### **Сценарий 2: Полная очистка и переключение**
1. Пользователь нажимает "Полный сброс"
2. Видит предупреждение о полном удалении данных
3. Подтверждает действие
4. Система удаляет:
   - Все чаты из базы данных
   - Все статусы прочитанности
   - Кэш аватарок
   - Папки .wwebjs_auth и .wwebjs_cache
5. Генерируется новый QR-код
6. Подключается совершенно "чистый" аккаунт

### **Сценарий 3: Обработка сообщений от "чужих" аккаунтов**
1. Приходит сообщение с номера, не относящегося к текущему аккаунту
2. Система проверяет принадлежность: `msg.to === currentAccountInfo.phoneNumber`
3. Если не совпадает - сообщение игнорируется с логом
4. В интерфейсе появляются только сообщения текущего аккаунта

---

## 📋 **API Endpoints**

### **GET /whatsapp/account**
```json
{
    "success": true,
    "account": {
        "phoneNumber": "77054404709@c.us",
        "name": "Иван Петров",
        "profilePicUrl": "https://...",
        "isReady": true,
        "connectedAt": "2024-01-15T10:30:00Z"
    },
    "hasActiveAccount": true
}
```

### **POST /whatsapp/reset**
```json
{
    "success": true,
    "message": "WhatsApp account reset completed. Please scan new QR code.",
    "requiresNewAuth": true
}
```

### **GET /whatsapp/chats-summary**
```json
{
    "success": true,
    "summary": {
        "totalChats": 15,
        "totalMessages": 245,
        "totalUnreadChats": 3,
        "currentAccount": "77054404709@c.us",
        "hasMultipleAccountData": false,
        "readStatusEntries": 15
    },
    "accountInfo": { ... }
}
```

---

## 🚨 **Безопасность и предупреждения**

### **🔴 Критические предупреждения:**
1. **Полный сброс необратим** - все данные удаляются безвозвратно
2. **Бэкап данных** - система не создает автоматические бэкапы
3. **Одновременные сессии** - поддерживается только один активный аккаунт

### **🟡 Рекомендации:**
1. Используйте "Выйти" для временного переключения
2. Используйте "Полный сброс" только при проблемах или окончательной смене
3. Регулярно проверяйте статистику чатов на наличие "чужих" данных

---

## 🐛 **Диагностика проблем**

### **Проблема: Сообщения от неизвестных номеров**
**Решение:**
1. Откройте "Управление аккаунтом"
2. Проверьте текущий активный аккаунт
3. Если есть "чужие" чаты - выполните "Полный сброс"
4. Пересканируйте QR-код

### **Проблема: Старые чаты не исчезают**
**Решение:**
1. Проверьте базу данных Supabase
2. Используйте `GET /whatsapp/chats-summary` для анализа
3. Выполните "Полный сброс" для очистки

### **Проблема: Не удается подключить новый аккаунт**
**Решение:**
1. Проверьте папки .wwebjs_auth и .wwebjs_cache
2. Выполните "Полный сброс"
3. Перезапустите сервер
4. Проверьте логи сервера на ошибки

---

## 📈 **Мониторинг и логи**

### **Ключевые логи для мониторинга:**
```
📱 Account info updated: { phoneNumber: "...", name: "..." }
🧹 Clearing all account data...
✅ All account data cleared
⚠️  Message not for current account, ignoring
🔗 Account connected: { ... }
🔄 Account reset: { ... }
```

### **Метрики для отслеживания:**
- Количество игнорируемых сообщений от "чужих" аккаунтов
- Частота сбросов аккаунтов
- Время между отключением и новым подключением
- Размер очищаемых данных

---

## 🎯 **Результат внедрения**

### **✅ Достигнутые цели:**
1. **Устранение путаницы** с источниками сообщений
2. **Четкий контроль** активного аккаунта
3. **Простое переключение** между аккаунтами
4. **Полная очистка** старых данных
5. **Прозрачность процесса** для пользователя

### **📊 Преимущества:**
- Нет смешивания данных от разных аккаунтов
- Понятный интерфейс управления
- Возможность диагностики проблем
- Гибкость в управлении данными
- Соблюдение принципа "один аккаунт - одна сессия"

Система теперь гарантирует работу с одним активным аккаунтом WhatsApp, предоставляя пользователю полный контроль над данными и возможность чистого переключения между аккаунтами! 🎉 