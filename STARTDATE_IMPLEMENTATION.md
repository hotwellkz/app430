# 🚀 Автоматическая установка даты начала строительства

## 📋 Описание изменений

Реализована автоматическая установка `startDate` при переводе клиента из категории "Задаток" в категорию "Строим", **интеллектуальный расчет даты старта** по введенному количеству оставшихся дней, а также **полноценное редактирование адреса строительства** на всех устройствах.

## 🔧 Внесенные изменения

### 1. Обновление логики смены статуса (`src/pages/Clients.tsx`)

```typescript
onStatusChange={(newStatus) => {
  if (selectedClient) {
    const clientRef = doc(db, 'clients', selectedClient.id);
    const updateData: any = {
      status: newStatus,
      updatedAt: serverTimestamp()
    };
    
    // Если переводим в "Строим" и startDate не установлена - устанавливаем текущую дату
    if (newStatus === 'building' && !selectedClient.startDate) {
      updateData.startDate = new Date().toISOString().split('T')[0]; // Формат YYYY-MM-DD
    }
    
    updateDoc(clientRef, updateData).then(() => {
      setShowContextMenu(false);
      showErrorNotification('Статус клиента обновлен');
    }).catch((error) => {
      console.error('Error updating client status:', error);
      showErrorNotification('Ошибка при обновлении статуса');
    });
  }
}}
```

### 2. Обновление расчета дней строительства (`src/components/clients/ClientCard.tsx`)

```typescript
const calculateBuildDaysLeft = () => {
  if (client.status !== 'building') return null;
  
  // Безопасное получение количества дней
  const totalDays = Number(client.buildDays) || Number(buildDays) || 45;
  
  // Используем startDate для расчета дней строительства
  const buildStartDate = client.startDate || client.categoryChangeDate || client.createdAt;
  if (!buildStartDate) return null;
  
  // Безопасный парсинг даты
  const startBuild = new Date(buildStartDate);
  if (isNaN(startBuild.getTime())) return null;
  
  const today = new Date();
  const daysPassed = Math.floor((today.getTime() - startBuild.getTime()) / (1000 * 60 * 60 * 24));
  const daysLeft = totalDays - daysPassed;
  
  // Возвращаем корректное число или 0, но не отрицательное
  return Math.max(0, daysLeft);
};
```

### 3. 🆕 Интеллектуальный расчет даты старта по оставшимся дням

```typescript
const handleRemainingDaysSubmit = async () => {
  const enteredDaysLeft = parseInt(remainingDaysInput);
  
  if (isNaN(enteredDaysLeft) || enteredDaysLeft < 0) {
    alert("Введите корректное число");
    return;
  }
  
  const totalBuildDays = client.buildDays || 45;
  const daysPassed = totalBuildDays - enteredDaysLeft;
  
  // Рассчитываем дату старта = сегодня минус количество прошедших дней
  const today = new Date();
  const calculatedStartDate = new Date(today);
  calculatedStartDate.setDate(today.getDate() - daysPassed);
  
  const startDateString = calculatedStartDate.toISOString().split('T')[0]; // Формат YYYY-MM-DD
  
  await updateDoc(doc(db, "clients", client.id), {
    startDate: startDateString,
    buildDays: totalBuildDays
  });
  
  // Локальное обновление UI
  setStartDate(startDateString);
};
```

### 4. Кликабельное предупреждение "⚠ Укажите дату старта"

```typescript
// Предупреждение теперь кликабельно и вызывает модальное окно
<span 
  key="buildDaysError"
  style={{
    fontSize: '10px',
    color: 'red',
    display: 'inline',
    cursor: 'pointer',
    fontWeight: '400'
  }}
  onClick={handleRemainingDaysClick}
>
  ⚠ Укажите дату старта
</span>
```

### 5. Обновление отображения для статуса "Строим"

```typescript
if (client.status === 'building') {
  // Для строительства - показываем дату старта и таймер дней
  if (client.startDate) {
    const startDateFormatted = new Date(client.startDate).toLocaleDateString('ru-RU');
    elements.push(
      <span 
        key="startDate"
        style={{
          fontSize: '10px',
          color: '#999',
          display: 'inline',
          cursor: 'pointer',
          fontWeight: '400'
        }}
        onClick={handleDateClick}
      >
        🏗 старт: {startDateFormatted}
      </span>
    );
  }
  
  if (buildDaysLeft !== null && !isNaN(buildDaysLeft)) {
    const isUrgent = buildDaysLeft <= 7;
    const daysText = `осталось: ${buildDaysLeft} дн.`;
    
    elements.push(
      <span 
        key="buildDays"
        style={{
          fontSize: '10px',
          color: '#999',
          display: 'inline',
          cursor: 'pointer',
          fontWeight: '400'
        }}
        onClick={handleBuildDaysClick}
      >
        <span 
          className={isUrgent ? 'blink' : ''}
          style={{
            color: isUrgent ? 'red' : 'inherit',
            fontWeight: isUrgent ? 'bold' : 'inherit'
          }}
        >
          {daysText}
        </span>
      </span>
    );
  }
}
```

## 🎯 Функциональность

### ✅ Что реализовано:

1. **Автоматическая установка startDate** - при переводе клиента в статус "Строим" автоматически устанавливается текущая дата как дата начала строительства
2. **Условная установка** - дата устанавливается только если `startDate` ещё не была указана ранее
3. **🆕 Интеллектуальный расчет даты старта** - по введенному количеству оставшихся дней автоматически вычисляется дата начала строительства
4. **Кликабельное предупреждение** - "⚠ Укажите дату старта" стало кликабельным и открывает модальное окно для ввода
5. **Корректный расчет дней** - функция `calculateBuildDaysLeft` использует `startDate` для точного расчета оставшихся дней строительства  
6. **Визуальное отображение** - для статуса "Строим" показывается:
   - 🏗 дата старта строительства (кликабельная для редактирования)
   - Таймер "осталось: X дн." с мигающим предупреждением при ≤7 днях
7. **Защита от NaN** - добавлена проверка корректности данных и fallback-сообщения
8. **Валидация ввода** - проверка корректности введенного числа с предупреждением "Введите корректное число"

### 🧠 Логика расчета:

```typescript
// Пример: пользователь ввел 30 оставшихся дней, общих дней строительства 45
const buildDays = 45;           // общее количество дней строительства
const enteredDaysLeft = 30;     // введенное количество оставшихся дней
const daysPassed = 45 - 30 = 15; // количество прошедших дней

// Дата старта = сегодня минус количество прошедших дней
const startDate = today - 15 дней;
```

### 📱 Адаптивность:

- Отображение работает на мобильных и десктопных устройствах
- Сохранены все существующие стили и анимации
- Поддержка редактирования через модальные окна
- Новое модальное окно для ввода оставшихся дней

### 🔧 Техническая реализация:

- Формат даты: `YYYY-MM-DD` (ISO стандарт)
- Безопасное преобразование типов
- Локальное обновление состояния для быстрого отклика UI
- Централизованное управление через Firebase Firestore
- Автоматический расчет с использованием JavaScript Date API

## 🧪 Тестирование

### Проверьте следующие сценарии:

#### 🏗 Автоматическая установка даты старта:
1. **Перевод из Задаток → Строим** - должна автоматически установиться дата начала
2. **Повторный перевод** - дата не должна изменяться если уже установлена

#### 🧠 Интеллектуальный расчет даты:
3. **🆕 Клик на "⚠ Укажите дату старта"** - должно открыться модальное окно для ввода оставшихся дней
4. **🆕 Ввод 30 дней при buildDays=45** - дата старта должна быть установлена на 15 дней назад от сегодня
5. **🆕 Проверка после ввода** - интерфейс должен показать "🏗 осталось: 30 дн."
6. **🆕 Валидация ввода** - при вводе некорректного числа должно показаться "Введите корректное число"
7. **🆕 Сохранение в Firebase** - проверить что startDate и buildDays корректно сохраняются в базе данных

#### ⏰ Отображение таймеров:
8. **Отсчет дней** - таймер должен показывать корректное количество оставшихся дней
9. **Мигание при ≤7 днях** - должно включиться красное мигающее предупреждение
10. **Редактирование даты** - возможность ручной корректировки через модальное окно

#### 🏠 Адрес строительства:
11. **🆕 Клик по адресу в десктопной версии** - должен открываться редактор адреса (как в мобильной)
12. **🆕 Курсор pointer при наведении** - на всех версиях должен показываться указатель
13. **🆕 Клик по "📍 Добавить адрес"** - должно открываться модальное окно для добавления нового адреса
14. **🆕 Клик по "—" в статусе "built"** - должен открывать редактор для добавления адреса
15. **🆕 Сокращение адреса на мобильных** - длинные адреса (>15 символов) должны сокращаться с "..."
16. **🆕 Полное отображение на десктопе** - адрес должен показываться целиком
17. **🆕 Сохранение адреса** - изменения должны сохраняться в Firebase и отображаться без перезагрузки
18. **🆕 Динамический заголовок модального окна** - "Редактировать" или "Добавить" в зависимости от наличия адреса

## 🔄 Обратная совместимость

Все изменения обратно совместимы:
- Существующие клиенты без `startDate` продолжают работать
- Старая логика расчета дней сохранена как fallback
- Интерфейс Client уже содержал необходимые поля
- Новая функциональность расчета по оставшимся дням добавлена как дополнительная опция

## 🎨 Стили

Все необходимые CSS стили уже присутствуют в `src/index.css`:
- `.blink` - анимация мигания для критических дат
- `.address-modal-overlay` и `.address-modal-content` - стили модальных окон
- Адаптивные медиа-запросы для мобильных устройств

## 🆕 Новые модальные окна

1. **Модальное окно выбора даты старта** - для ручного ввода даты начала строительства
2. **Модальное окно редактирования количества дней** - для изменения общего количества дней строительства  
3. **🆕 Модальное окно ввода оставшихся дней** - для автоматического расчета даты старта по оставшимся дням
4. **🆕 Универсальное модальное окно редактирования адреса** - для добавления/редактирования адреса строительства на всех устройствах

## 🏗 Адрес строительства - полная функциональность

### ✅ Что реализовано для адреса:

1. **Кликабельный адрес в обеих версиях** - как в мобильной, так и в десктопной версии адрес можно редактировать кликом
2. **Автоматическое сокращение на мобильных** - длинные адреса (>15 символов) сокращаются с многоточием
3. **Полное отображение на десктопе** - адрес показывается целиком
4. **Кликабельный элемент добавления** - если адрес не указан, показывается "📍 Добавить адрес"
5. **Единое модальное окно** - одно модальное окно работает для всех устройств и статусов
6. **Динамический заголовок** - "Редактировать адрес строительства" или "Добавить адрес строительства"
7. **Подсказка в placeholder** - примеры правильного заполнения адреса

### 🎯 Логика отображения адреса:

```typescript
// Статус "built" - всегда показывает адрес или кликабельный прочерк
if (client.status === 'built') {
  return client.constructionAddress 
    ? <ClickableAddress>{address}</ClickableAddress>
    : <ClickableEmptyAddress>—</ClickableEmptyAddress>;
}

// Статусы "deposit" и "building" - адрес среди других элементов
if (client.constructionAddress) {
  return <ClickableAddress>{address}</ClickableAddress>;
} else {
  return <ClickableAddButton>📍 Добавить адрес</ClickableAddButton>;
}
```

### 🖱 Взаимодействие:

- **Наведение курсора** - показывает `pointer` на всех устройствах
- **Клик по адресу** - открывает модальное окно для редактирования
- **Клик по "📍 Добавить адрес"** - открывает модальное окно для добавления
- **Клик по "—"** - в статусе "built" также открывает модальное окно

### 📱 Адаптивность адреса:

- **Мобильная версия**: сокращение до 15 символов + "..."
- **Десктопная версия**: полное отображение адреса
- **Единое модальное окно**: работает одинаково на всех устройствах
- **Responsive layout**: корректное отображение в разных разрешениях

## 🔧 Техническая реализация адреса

### Удаленные ограничения:

```typescript
// ❌ Старая логика - только для мобильных
if (isMobile && client.constructionAddress) {
  setShowAddressModal(true);
}

// ✅ Новая логика - для всех устройств
setNewAddress(client.constructionAddress || '');
setShowAddressModal(true);
```

### Обновленные условия:

```typescript
// ❌ Было: только для мобильных с адресом
{showAddressModal && isMobile && client.constructionAddress && (

// ✅ Стало: для всех устройств и случаев
{showAddressModal && (
``` 