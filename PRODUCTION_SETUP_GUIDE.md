# 🚀 Полное руководство по настройке Production-сервера

## 🎯 Цель
Настроить стабильный production-сервер на Ubuntu (Google Cloud VM) для Backend и Frontend (Vite + React + Node.js/Express) с доменом **2wix.ru**

## 📋 Что будет настроено

- ✅ **Google Cloud Firewall** - открытие портов 80, 443, 3000
- ✅ **Привязка домена** - настройка DNS для 2wix.ru
- ✅ **NGINX с SSL** - проксирование на backend + Let's Encrypt
- ✅ **WebSocket поддержка** - для Socket.IO соединений
- ✅ **CORS настройки** - корректная работа с frontend
- ✅ **Docker развертывание** - стабильная контейнеризация
- ✅ **Автопродление SSL** - через cron задачи
- ✅ **Мониторинг и диагностика** - инструменты для отладки

## 🛠️ Требования

### Сервер:
- **Google Cloud VM** с Ubuntu 20.04+
- **RAM**: минимум 2GB, рекомендуется 4GB
- **CPU**: минимум 1 vCPU, рекомендуется 2 vCPU
- **Disk**: минимум 20GB SSD
- **External IP** адрес

### Домен:
- Домен **2wix.ru** должен указывать на IP вашей VM
- Доступ к настройкам DNS

## 🚀 Быстрый старт

### 1. Подключение к серверу

```bash
# Подключитесь к вашей Google Cloud VM
ssh username@YOUR_EXTERNAL_IP

# Убедитесь, что система обновлена
sudo apt update && sudo apt upgrade -y
```

### 2. Получение проекта

```bash
# Если проект уже на сервере, перейдите в директорию
cd /path/to/your/project

# Или клонируйте из репозитория
git clone https://github.com/your-repo/app343-main-main.git
cd app343-main-main
```

### 3. Полная автоматическая настройка

```bash
# Сделайте скрипт исполняемым
chmod +x production-setup-complete.sh

# Запустите полную настройку (требуются права sudo)
./production-setup-complete.sh
```

**Скрипт автоматически выполнит:**
1. 🔒 Настройку Google Cloud Firewall
2. 🔒 Настройку локального firewall (UFW)
3. 🌍 Установку и настройку NGINX
4. 🔐 Получение SSL сертификата через Let's Encrypt
5. 🧠 Настройку переменных окружения
6. 🐳 Установку Docker и Docker Compose
7. 🔁 Запуск и проверку сервера
8. ✅ Полную диагностику

## 🔧 Пошаговая настройка (если нужна кастомизация)

### Шаг 1: Настройка переменных окружения Frontend

```bash
# Настройка frontend для работы с доменом
chmod +x update-frontend-env.sh
./update-frontend-env.sh
```

### Шаг 2: Настройка DNS (ВАЖНО!)

**Перед запуском SSL убедитесь, что:**
1. Домен `2wix.ru` указывает на IP вашей VM
2. Домен `www.2wix.ru` тоже указывает на IP вашей VM

Проверить можно командой:
```bash
nslookup 2wix.ru
# Должен показать IP вашей VM
```

### Шаг 3: Ручная настройка компонентов (если нужно)

```bash
# Только NGINX (без SSL)
sudo apt update && sudo apt install -y nginx

# Только SSL
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 2wix.ru -d www.2wix.ru

# Только Docker
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
```

## 🔍 Диагностика и отладка

### Быстрая диагностика

```bash
# Полная диагностика сервера
chmod +x diagnose-server.sh
./diagnose-server.sh
```

### Проверка отдельных компонентов

```bash
# Проверка backend
curl http://localhost:3000/health

# Проверка через домен
curl https://2wix.ru/health

# Статус Docker контейнеров
docker-compose -f docker-compose.production.yml ps

# Логи в реальном времени
docker-compose -f docker-compose.production.yml logs -f

# Статус NGINX
sudo systemctl status nginx

# Проверка SSL сертификатов
sudo certbot certificates
```

## 🛠️ Управление сервером

### Основные команды

```bash
# Запуск сервера
docker-compose -f docker-compose.production.yml up -d

# Остановка сервера
docker-compose -f docker-compose.production.yml down

# Перезапуск сервера
docker-compose -f docker-compose.production.yml restart

# Обновление и перезапуск
docker-compose -f docker-compose.production.yml up -d --build

# Просмотр логов
docker-compose -f docker-compose.production.yml logs -f
```

### Управление NGINX

```bash
# Перезапуск NGINX
sudo systemctl restart nginx

# Проверка конфигурации
sudo nginx -t

# Статус NGINX
sudo systemctl status nginx

# Логи NGINX
sudo tail -f /var/log/nginx/error.log
```

## 🔒 Настройки безопасности

### Firewall статус

```bash
# Проверка UFW
sudo ufw status verbose

# Проверка открытых портов
netstat -tlnp | grep -E "(80|443|3000)"
```

### SSL мониторинг

```bash
# Проверка срока действия сертификата
sudo certbot certificates

# Тест автопродления
sudo certbot renew --dry-run
```

## 📱 Настройки Frontend

После успешной настройки сервера, обновите настройки вашего frontend:

### Переменные окружения (.env)

```bash
VITE_BACKEND_URL=https://2wix.ru
VITE_API_URL=https://2wix.ru
VITE_SOCKET_URL=https://2wix.ru
VITE_ENVIRONMENT=production
```

### В коде приложения

```typescript
// src/config/environment.ts
export const config = {
  API_URL: 'https://2wix.ru',
  BACKEND_URL: 'https://2wix.ru',
  SOCKET_URL: 'https://2wix.ru',
  ENVIRONMENT: 'production'
};
```

## 🚨 Решение частых проблем

### Проблема: Backend не запускается

**Диагностика:**
```bash
docker-compose -f docker-compose.production.yml logs
```

**Решение:**
```bash
# Пересоздание контейнеров
docker-compose -f docker-compose.production.yml down
docker-compose -f docker-compose.production.yml up -d --build
```

### Проблема: SSL сертификат не получается

**Причины:**
- Домен не указывает на IP сервера
- Порт 80 заблокирован
- NGINX конфигурация некорректна

**Решение:**
```bash
# Проверьте DNS
nslookup 2wix.ru

# Проверьте NGINX
sudo nginx -t

# Попробуйте получить сертификат снова
sudo certbot --nginx -d 2wix.ru -d www.2wix.ru
```

### Проблема: CORS ошибки

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep ALLOWED_ORIGINS

# Обновите CORS настройки
echo "ALLOWED_ORIGINS=https://2wix.ru,https://www.2wix.ru" >> .env

# Перезапустите backend
docker-compose -f docker-compose.production.yml restart
```

### Проблема: WebSocket соединения не работают

**Проверка:**
```bash
# В браузере на вашем frontend сайте
const socket = io('https://2wix.ru');
socket.on('connect', () => console.log('Connected!'));
```

**Решение:**
- Убедитесь, что NGINX правильно проксирует WebSocket
- Проверьте, что в NGINX конфигурации есть секция `/socket.io/`

## 📊 Мониторинг производительности

### Ресурсы сервера

```bash
# Использование памяти
free -h

# Использование диска
df -h

# Загрузка CPU
htop

# Статистика Docker контейнеров
docker stats
```

### Мониторинг логов

```bash
# Логи в реальном времени
tail -f /var/log/nginx/access.log
docker-compose -f docker-compose.production.yml logs -f

# Системные логи
sudo journalctl -f
```

## 🔄 Автоматизация и бэкапы

### Автоматическое обновление

```bash
# Создайте скрипт обновления
cat > update-server.sh << 'EOF'
#!/bin/bash
cd /path/to/your/project
git pull
docker-compose -f docker-compose.production.yml up -d --build
EOF

chmod +x update-server.sh
```

### Настройка cron задач

```bash
# Редактирование crontab
crontab -e

# Добавьте строки для автоматизации:
# Обновление SSL каждые 12 часов
0 */12 * * * /usr/bin/certbot renew --quiet

# Очистка Docker логов каждую неделю
0 0 * * 0 docker system prune -f
```

## ✅ Финальная проверка

После настройки убедитесь, что всё работает:

- [ ] ✅ `https://2wix.ru/health` отвечает 200 OK
- [ ] ✅ Frontend загружается с домена  
- [ ] ✅ Backend API доступен через HTTPS
- [ ] ✅ WebSocket соединения стабильны
- [ ] ✅ CORS не блокирует запросы
- [ ] ✅ SSL сертификат валиден
- [ ] ✅ Автопродление SSL настроено
- [ ] ✅ Docker контейнеры запущены и стабильны

## 🎉 Готово!

Ваш production-сервер настроен и готов к работе:

- **🌐 Домен**: https://2wix.ru
- **🔗 API**: https://2wix.ru  
- **🔌 WebSocket**: https://2wix.ru/socket.io/
- **🔒 SSL**: Автоматическое продление
- **🐳 Docker**: Стабильная контейнеризация
- **🌍 Доступность**: Из любой точки мира

## 📞 Поддержка

При возникновении проблем:

1. **Запустите диагностику**: `./diagnose-server.sh`
2. **Проверьте логи**: `docker-compose -f docker-compose.production.yml logs -f`
3. **Проверьте статус сервисов**: `sudo systemctl status nginx docker`

Ваш сервер теперь работает без ngrok и полностью готов для production-нагрузки! 🚀 