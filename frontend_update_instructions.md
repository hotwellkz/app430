# 🔧 Инструкции по обновлению Frontend

## ✅ Backend готов!
- **URL**: `https://2ac0-35-194-39-8.ngrok-free.app`
- **Статус**: ✅ Работает
- **API**: ✅ Отвечает
- **CORS**: ✅ Настроен для `https://2wix.ru`

## 🔗 Обновите Frontend конфигурацию

### 1. В файле `src/config/api.ts`:
```typescript
export const API_BASE_URL = 'https://2ac0-35-194-39-8.ngrok-free.app'
export const WEBSOCKET_URL = 'https://2ac0-35-194-39-8.ngrok-free.app'
```

### 2. Или в переменных окружения `.env`:
```bash
VITE_API_BASE_URL=https://2ac0-35-194-39-8.ngrok-free.app
VITE_WEBSOCKET_URL=https://2ac0-35-194-39-8.ngrok-free.app
```

### 3. Пересоберите frontend:
```bash
npm run build
# или
yarn build
```

### 4. Перезапустите frontend сервер:
```bash
npm run dev
# или
yarn dev
```

## 🧪 Тестирование подключения

1. **Откройте**: `https://2wix.ru/whatsapp`
2. **Проверьте консоль**: Должны исчезнуть CORS ошибки
3. **WebSocket**: Должен подключиться к правильному URL
4. **API вызовы**: Должны работать без ошибок

## 📱 WhatsApp инициализация

Backend продолжает инициализацию WhatsApp клиента. Через 1-2 минуты в логах появится QR код для подключения:

```bash
# Следите за логами
docker logs whatsapp-backend-prod -f
```

## 🔄 Если ngrok URL изменится

При перезапуске ngrok URL может измениться. В таком случае:

1. **Получите новый URL**:
   ```bash
   curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url'
   ```

2. **Обновите .env на сервере**:
   ```bash
   # Замените URL в CORS_ORIGIN
   nano .env
   ```

3. **Перезапустите backend**:
   ```bash
   docker-compose restart
   ```

4. **Обновите frontend** с новым URL

## ✅ Текущий статус
- 🟢 Backend: Работает
- 🟢 API: Доступен
- 🟢 HTTPS: Активен
- 🟡 WhatsApp: Инициализируется
- ⏳ Frontend: Требует обновления 