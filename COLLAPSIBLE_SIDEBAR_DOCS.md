# 📂 Сворачиваемое боковое меню (Collapsible Sidebar)

## 🎯 **Описание функциональности**

Реализована возможность сворачивания левого бокового меню для экономии места на экране и улучшения UX.

## ✅ **Возможности**

### 🔧 **Основные функции**
- ✅ **Сворачивание/разворачивание** меню кнопкой
- ✅ **Сохранение состояния** в localStorage
- ✅ **Адаптивные анимации** (300ms cubic-bezier)
- ✅ **Tooltip подсказки** в свернутом режиме
- ✅ **Мобильная совместимость**
- ✅ **Автоматическая адаптация контента**

### 📱 **Режимы отображения**

#### **Развернутое состояние (Expanded)**
- Ширина: `256px` (w-64)
- Показываются: иконки + текст
- Полная функциональность меню

#### **Свернутое состояние (Collapsed)**  
- Ширина: `64px` (w-16)
- Показываются: только иконки
- Tooltip при наведении
- Центрированные элементы

## 🎨 **UI/UX особенности**

### **Кнопка управления**
```tsx
<button onClick={toggleSidebar}>
  {collapsed ? <ChevronRight /> : <ChevronLeft />}
</button>
```
- Расположение: правый верхний угол sidebar
- Только на десктопе (скрыта на мобильных)
- Плавные анимации иконок

### **Tooltip система**
```tsx
{collapsed && showTooltip && (
  <div className="tooltip">
    {item.label}
    <div className="tooltip-arrow" />
  </div>
)}
```
- Появляются при наведении в collapsed режиме
- Темная тема с белым текстом
- Стрелка указывает на элемент

### **Анимации**
- **Ширина sidebar**: `transition-all duration-300 ease-in-out`
- **Контент**: `cubic-bezier(0.4, 0, 0.2, 1)`
- **Tooltip**: `opacity transition 200ms`
- **Иконки**: `scale(1.1)` при hover

## 🛠 **Техническая реализация**

### **Структура компонентов**

#### **1. Основной Sidebar**
```tsx
// src/components/Sidebar.tsx
const [collapsed, setCollapsed] = useState<boolean>(() => {
  return localStorage.getItem('sidebar-collapsed') === 'true';
});

const toggleSidebar = () => {
  const newState = !collapsed;
  setCollapsed(newState);
  localStorage.setItem('sidebar-collapsed', newState.toString());
};
```

#### **2. MenuItem компонент**
```tsx
interface MenuItemComponentProps {
  item: MenuItem;
  onClick: () => void;
  collapsed: boolean;
}

const MenuItemComponent: React.FC<MenuItemComponentProps> = ({ 
  item, onClick, collapsed 
}) => {
  const [showTooltip, setShowTooltip] = useState(false);
  // ... реализация
};
```

#### **3. Адаптация основного layout**
```tsx
// src/App.tsx
<div className={`transition-all duration-300 ease-in-out ${
  collapsed ? 'lg:pl-16' : 'lg:pl-64'
}`}>
  <Header />
  <Routes>...</Routes>
</div>
```

### **Состояние и синхронизация**

#### **localStorage интеграция**
```tsx
// Чтение при инициализации
const [collapsed, setCollapsed] = useState<boolean>(() => {
  return localStorage.getItem('sidebar-collapsed') === 'true';
});

// Сохранение при изменении
const toggleSidebar = () => {
  const newState = !collapsed;
  setCollapsed(newState);
  localStorage.setItem('sidebar-collapsed', newState.toString());
};
```

#### **Синхронизация между табами**
```tsx
useEffect(() => {
  const handleStorageChange = () => {
    setCollapsed(localStorage.getItem('sidebar-collapsed') === 'true');
  };

  window.addEventListener('storage', handleStorageChange);
  
  // Polling для изменений внутри таба
  const interval = setInterval(checkCollapsedState, 100);

  return () => {
    window.removeEventListener('storage', handleStorageChange);
    clearInterval(interval);
  };
}, [collapsed]);
```

## 🎭 **CSS стили**

### **Основные анимации**
```css
/* Переходы sidebar */
.sidebar-transition {
  transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.sidebar-content-transition {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Tooltip анимации */
.tooltip-animation {
  transition: opacity 0.2s ease-in-out;
}
```

### **Иконки и взаимодействие**
```css
.icon-hover-scale {
  transition: transform 0.2s ease-in-out;
}

.icon-hover-scale:hover {
  transform: scale(1.1);
}

.button-press-animation:active {
  transform: scale(0.95);
}
```

## 📱 **Мобильная адаптация**

### **Поведение на мобильных**
- Кнопка сворачивания **скрыта** на мобильных
- Collapse состояние **не влияет** на мобильную версию
- Мобильное меню остается в **overlay режиме**

### **Медиа-запросы**
```css
@media (max-width: 1024px) {
  .sidebar-transition {
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
}
```

## 🔧 **Настройки и кастомизация**

### **Изменение размеров**
```tsx
// Ширина в collapsed режиме
const COLLAPSED_WIDTH = 'w-16'; // 64px

// Ширина в expanded режиме  
const EXPANDED_WIDTH = 'w-64'; // 256px

// Отступ для контента
const CONTENT_PADDING_COLLAPSED = 'lg:pl-16';
const CONTENT_PADDING_EXPANDED = 'lg:pl-64';
```

### **Тайминги анимаций**
```tsx
// Основная анимация sidebar
const SIDEBAR_TRANSITION = 'duration-300';

// Tooltip появление
const TOOLTIP_TRANSITION = 'duration-200';

// Hover эффекты
const HOVER_TRANSITION = 'duration-200';
```

### **Цветовая схема**
```tsx
// Активный элемент
const ACTIVE_COLORS = 'bg-emerald-50 text-emerald-600';

// Hover состояние
const HOVER_COLORS = 'hover:bg-gray-50 hover:text-emerald-600';

// Tooltip
const TOOLTIP_COLORS = 'bg-gray-900 text-white';
```

## 🧪 **Тестирование**

### **Функциональные тесты**
1. ✅ Кнопка сворачивания работает
2. ✅ Состояние сохраняется в localStorage
3. ✅ Анимации плавные без рывков
4. ✅ Tooltip появляются при наведении
5. ✅ Контент адаптируется к ширине
6. ✅ Мобильная версия не затронута

### **Кроссбраузерность**
- ✅ Chrome/Edge (Webkit)
- ✅ Firefox
- ✅ Safari
- ✅ Mobile Safari
- ✅ Chrome Mobile

### **Производительность**
- ✅ Анимации на GPU (transform/opacity)
- ✅ Минимальный reflow
- ✅ CSS transitions вместо JS анимаций
- ✅ Throttled localStorage проверка

## 📋 **Чеклист для разработчиков**

### **При добавлении новых пунктов меню**
- [ ] Добавить иконку в `menuItems`
- [ ] Убедиться что иконка центрируется в collapsed режиме
- [ ] Проверить tooltip текст
- [ ] Протестировать на мобильных

### **При изменении стилей**
- [ ] Проверить оба состояния (collapsed/expanded)
- [ ] Убедиться в совместимости с темной темой
- [ ] Протестировать анимации
- [ ] Проверить z-index конфликты

## 🚀 **Использование**

### **Пользователь**
1. Нажать кнопку `←` для сворачивания
2. Нажать кнопку `→` для разворачивания  
3. Навести на иконку для просмотра названия
4. Состояние сохраняется между сессиями

### **Разработчик**
```tsx
// Получить текущее состояние
const isCollapsed = localStorage.getItem('sidebar-collapsed') === 'true';

// Программно изменить состояние
localStorage.setItem('sidebar-collapsed', 'true');

// Слушать изменения
window.addEventListener('storage', handleStorageChange);
```

## 🔍 **Отладка**

### **Проблемы и решения**

#### **Tooltip не появляются**
```tsx
// Убедитесь что состояние collapsed передается
<MenuItemComponent collapsed={collapsed} />

// Проверьте z-index
.tooltip { z-index: 1050; }
```

#### **Анимации дергаются**
```css
/* Используйте will-change для GPU ускорения */
.sidebar-transition {
  will-change: width;
}
```

#### **localStorage не синхронизируется**
```tsx
// Добавьте polling для внутри-табовых изменений
useEffect(() => {
  const interval = setInterval(() => {
    const current = localStorage.getItem('sidebar-collapsed') === 'true';
    if (current !== collapsed) {
      setCollapsed(current);
    }
  }, 100);
  
  return () => clearInterval(interval);
}, [collapsed]);
```

## 🔮 **Планы развития**

### **Возможные улучшения**
- [ ] Автосворачивание на маленьких экранах
- [ ] Настройка анимационных переходов
- [ ] Сохранение в профиле пользователя
- [ ] Горячие клавиши (Ctrl+B)
- [ ] Анимированные иконки
- [ ] Темы для tooltip

### **A/B тестирование**
- [ ] Оптимальная ширина collapsed режима
- [ ] Время показа tooltip
- [ ] Позиционирование кнопки управления 