# 🔧 Настройка переменных окружения

## 📋 Для подключения фронтенда к backend на VM

### 1. Создайте файл `.env.production` в корне проекта:

```bash
# Backend API URL - замените на ваш внешний IP Google Cloud VM
VITE_BACKEND_URL=http://YOUR_EXTERNAL_IP:3000
VITE_API_URL=http://YOUR_EXTERNAL_IP:3000
```

### 2. Замените `YOUR_EXTERNAL_IP` на реальный IP:

Например, если ваш внешний IP: `35.194.39.8`

```bash
VITE_BACKEND_URL=http://35.194.39.8:3000
VITE_API_URL=http://35.194.39.8:3000
```

### 3. Для локальной разработки создайте `.env.local`:

```bash
VITE_BACKEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000
```

## 🚀 Сборка и деплой

После создания `.env.production`:

```bash
# Соберите production версию
npm run build

# Задеплойте на ваш хостинг (например, Netlify/Vercel)
```

## 🔍 Проверка

Откройте консоль браузера на https://2wix.ru и увидите:
```
🔗 Backend URL: http://YOUR_EXTERNAL_IP:3000
🔌 Socket URL: http://YOUR_EXTERNAL_IP:3000
```

## ⚠️ Важно

1. **Service Worker**: Если возникают проблемы, временно отключите:
   ```javascript
   // В консоли браузера на https://2wix.ru
   swDisable()
   ```

2. **CORS**: Убедитесь что backend настроен для домена https://2wix.ru

3. **Firewall**: Проверьте что порт 3000 открыт в Google Cloud 