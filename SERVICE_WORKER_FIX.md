# 🛠️ Исправление Service Worker для API запросов

## Проблема
Service Worker блокирует HTTP запросы к localhost из HTTPS окружения.

## Решения

### 1. Быстрое - Отключить Service Worker
```javascript
// В консоли браузера (F12):
swDisable()
// Перезагрузите страницу
```

### 2. Настроить Service Worker для пропуска API

В файле `sw.js` фронтенда добавьте исключения:

```javascript
// В начало файла sw.js добавьте:
const API_PATTERNS = [
    /^http:\/\/localhost:\d+\//,
    /^http:\/\/127\.0\.0\.1:\d+\//,
    /^https:\/\/.*\.ngrok\.io\//,
];

// В обработчике fetch:
self.addEventListener('fetch', (event) => {
    const url = new URL(event.request.url);
    
    // Пропускаем API запросы
    if (API_PATTERNS.some(pattern => pattern.test(event.request.url))) {
        return; // Не перехватываем API запросы
    }
    
    // Остальная логика Service Worker...
});
```

### 3. Использовать ngrok (рекомендуется)
```bash
ngrok http 3000
# Получить HTTPS URL и использовать его вместо localhost
```

## Проверка
После исправления запросы к API должны проходить без ошибок Service Worker. 